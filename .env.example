# Frontend (Next.js) environment.

# Base URL of the Cushion indexer API (D3). The vault dashboard reads TVL,
# allocation, performance, and positions from here.
NEXT_PUBLIC_INDEXER_URL="http://localhost:8080"
