[workspace]
resolver = "2"
members = ["contracts/strategy-vault", "contracts/test-router", "contracts/test-oracle"]

[profile.release]
opt-level = "z"
lto = true
strip = "symbols"
panic = "abort"
overflow-checks = true
